import { View, Text } from "react-native";

const DeviceDataLog = ({ deviceId } : { deviceId: string }) => {
    return (
        <View>
            <Text>Device Data Log</Text>
        </View>
    );
}

export default DeviceDataLog;