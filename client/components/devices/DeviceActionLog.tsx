import { View, Text } from "react-native";

const DeviceActionLog = ({ deviceId } : { deviceId: string }) => {
    return (
        <View>
            <Text>Device Action Log</Text>
        </View>
    );
}

export default DeviceActionLog;