import { View , Text} from "react-native";

const DoorComponent = ({device}: {device: DeviceResponse}) => {
    return (
        <View>
            <Text>Door Component</Text>
        </View>
    );
}

export default DoorComponent;