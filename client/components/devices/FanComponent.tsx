import { View , Text} from "react-native";

const FanComponent = ({device}: {device: DeviceResponse}) => {
    return (
        <View>
            <Text>Fan Component</Text>
        </View>
    );
}

export default FanComponent;