import { View , Text} from "react-native";

const SensorComponent = ({device}: {device: DeviceResponse}) => {
    return (
        <View>
            <Text>Sensor Component</Text>
        </View>
    );
}

export default SensorComponent;