import { View , Text} from "react-native";

const LightComponent = ({device}: {device: DeviceResponse}) => {
    return (
        <View>
            <Text>Light Component</Text>
        </View>
    );
}

export default LightComponent;